# 2A202601345 Report Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tạo báo cáo Assignment 11 trung thực, đầy đủ rubric và liên kết đúng tới evidence hiện có.

**Architecture:** Dùng báo cáo Markdown gồm các phần theo rubric A/B. Mọi số liệu được đối chiếu từ các JSON trong `outputs/`; các kết quả thiếu, quota error và placeholder được ghi rõ như giới hạn.

**Tech Stack:** Markdown, JSON artifacts, shell read-only checks.

## Global Constraints

- Chỉ sửa `report/2A202601345_report.md`.
- Không sửa code nguồn hoặc artifact JSON.
- Không đưa API key, password, API key giả, DB host hoặc PII vào báo cáo.
- Không tuyên bố AI-generated attacks hoặc LLM judge đã hoàn thành khi evidence không chứng minh.

---

### Task 1: Tạo báo cáo theo rubric

**Files:**
- Create: `report/2A202601345_report.md`
- Read: `outputs/results.json`, `outputs/metrics.json`, `outputs/audit_log.json`, `outputs/attack_results.json`, `outputs/unsafe_attack_result.json`, `outputs/guards_attack_result.json`, `outputs/test_metrics.json`

**Interfaces:**
- Consumes: JSON artifacts và kết quả Part 2/4 được cung cấp.
- Produces: Báo cáo Markdown dùng MSSV `2A202601345`.

- [ ] **Step 1: Viết phần kiến trúc và mục tiêu**

Mô tả pipeline `rate limiter → input guardrail → LLM → output guardrail/judge → audit/monitoring → response/action gateway`, provenance của email/RAG và nguyên tắc LLM không tự quyết egress policy.

- [ ] **Step 2: Viết phần kết quả phòng thủ**

Ghi chính xác 5 safe queries được cho qua, 7 direct attacks bị `input_guardrail` chặn, 3 edge cases bị chặn, rate limit 10/15 qua và 5/15 bị chặn. Nêu output redaction cho API key, phone và email dựa trên Part 2.

- [ ] **Step 3: Viết phần action safety, HITL, audit và monitoring**

Mô tả allowlist HTTPS exact-host, reject secret/PII và high-risk fail-closed. Ghi 3 decision points HITL. Đối chiếu `metrics.json` với `test_metrics.json`, phân biệt suite chính có `judge_checks=0` với test bổ sung có judge evidence.

- [ ] **Step 4: Viết phần red-team và giới hạn**

Ghi unsafe 0/5 leak, guards 0/5 leak và 5/5 blocked input theo artifact. Ghi AI-generated attacks là 0, NeMo lỗi, Gemini 429 và prompt còn placeholder `TODO`; không xem các dòng lỗi/quota là bằng chứng block thành công.

- [ ] **Step 5: Viết phần production, đạo đức và evidence links**

Trả lời scale khoảng 10.000 user, false positive/trade-off, 2–3 attack có thể còn lọt, source-to-sink case, đạo đức “an toàn tuyệt đối”, rồi liên kết tương đối tới các file output.

### Task 2: Kiểm tra báo cáo

**Files:**
- Read: `report/2A202601345_report.md`

- [ ] **Step 1: Kiểm tra placeholder và secret**

Chạy:

```bash
rg -n "TODO|TBD|AQ\.|admin123|sk-vinbank|db\.vinbank|090[0-9]" report/2A202601345_report.md
```

Expected: không có output.

- [ ] **Step 2: Kiểm tra liên kết evidence**

Chạy:

```bash
for f in outputs/results.json outputs/metrics.json outputs/audit_log.json outputs/attack_results.json; do test -f "$f"; done
```

Expected: exit code 0.

- [ ] **Step 3: Kiểm tra trạng thái thay đổi**

Chạy `git diff -- report/2A202601345_report.md` và xác nhận chỉ báo cáo được tạo/cập nhật; không đụng code hoặc JSON.
