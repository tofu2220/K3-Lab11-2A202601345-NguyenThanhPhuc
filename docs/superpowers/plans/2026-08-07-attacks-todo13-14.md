# Attacks TODO 13–14 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Hoàn thiện prompt red-team thủ công và parser AI-generated attacks mà không giả lập kết quả AI.

**Architecture:** `adversarial_prompts` chứa 5 prompt cụ thể thuộc các kỹ thuật khác nhau. `generate_ai_attacks()` vẫn gọi Gemini thật, parse JSON array, lọc record hợp lệ và trả `[]` khi lỗi/quota/JSON không hợp lệ.

**Tech Stack:** Python, Gemini API, JSON.

## Global Constraints

- Chỉ sửa `src/attacks/attacks.py` trong phần chức năng.
- Không thay response agent bằng transcript tự tạo.
- Không tự đánh dấu `leaked`; leak chỉ do response runtime quyết định.
- Không dùng prompt fallback thủ công để giả làm AI-generated attacks.

---

### Task 1: Implement TODO 13–14

**Files:**
- Modify: `src/attacks/attacks.py`

- [ ] Thay 5 placeholder bằng prompt nâng cao cho completion, indirect email/RAG, obfuscation, authority/roleplay và output-format/multi-step.
- [ ] Parse JSON array từ Gemini, lọc object có `prompt` không rỗng, chuẩn hóa `type`, `target`, `why_it_works`, giới hạn tối thiểu theo dữ liệu thực trả về.
- [ ] Giữ lỗi/quota/parse failure là danh sách rỗng và in nguyên nhân.
- [ ] Chạy compile/import check và kiểm tra không còn placeholder trong `adversarial_prompts`.
