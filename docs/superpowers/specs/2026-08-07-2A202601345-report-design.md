# Thiết kế báo cáo Assignment 11 — 2A202601345

## Mục tiêu

Tạo lại `report/2A202601345_report.md` từ các artifact hiện có trong `outputs/`, đáp ứng các câu hỏi báo cáo của Assignment 11 mà không suy diễn hoặc bịa kết quả chưa được chạy.

## Phạm vi

- Tạo/cập nhật duy nhất file báo cáo của sinh viên.
- Đối chiếu các nhận định với `results.json`, `metrics.json`, `audit_log.json`, các file attack và output kiểm thử.
- Không sửa code nguồn hoặc thay đổi nội dung artifact JSON.
- Không đưa API key, credential, secret hoặc dữ liệu nhạy cảm vào báo cáo.

## Nội dung báo cáo

Báo cáo gồm: mục tiêu và kiến trúc; kết quả phòng thủ; action safety/egress/HITL; audit và monitoring; red-team; lỗ hổng còn lại; thiết kế khi scale khoảng 10.000 người dùng; đạo đức/disclaimer; danh sách evidence; kết luận.

## Quy tắc trung thực bằng chứng

- Ghi nhận 5 safe queries qua, 7 direct attacks bị input guardrail chặn, rate limit 10/15 qua và 5/15 bị chặn.
- Ghi output redaction dựa trên kết quả chạy Part 2.
- Ghi `judge_checks=0` trong suite chính; chỉ mô tả `test_metrics.json` là test bổ sung.
- Ghi AI-generated attacks bằng 0, NeMo lỗi, Gemini quota 429 và prompt red-team còn placeholder `TODO`.
- Ghi guards agent không leak trong các lần chạy đã lưu, không tuyên bố an toàn tuyệt đối.
- Nêu artifact dùng `SE00000` và báo cáo dùng MSSV chính thức `2A202601345`.

## Kiểm tra sau khi viết

Rà soát placeholder, mâu thuẫn số liệu, phát biểu vượt quá evidence và liên kết tới các file output. Báo cáo không chứa API key hoặc secret.
